- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:

    ~/.config/nvim/init.vim: neovim/init.vim
    ~/.config/nvim/pack/minpac/opt/minpac: neovim/minpac
    ~/.vimrc: neovim/vimrc

    ~/.zlogin: prezto/runcoms/zlogin
    ~/.zlogout: prezto/runcoms/zlogout
    ~/.zprezto: prezto
    ~/.zpreztorc:
    ~/.zprofile:
    ~/.zshenv: prezto/runcoms/zshenv
    ~/.zshrc:

    ~/.p10k.zsh:
