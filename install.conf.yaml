- defaults:
    link:
      create: true
      relink: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:

    ~/.config/nvim/init.vim: neovim/init.vim
    ~/.local/share/dein/repos/github.com/Shougo/dein.vim: neovim/dein.vim

    ~/.zlogin: prezto/runcoms/zlogin
    ~/.zlogout: prezto/runcoms/zlogout
    ~/.zprezto: prezto
    ~/.zpreztorc: prezto/runcoms/zpreztorc
    ~/.zprofile:
    ~/.zshenv: prezto/runcoms/zshenv
    ~/.zshrc:
